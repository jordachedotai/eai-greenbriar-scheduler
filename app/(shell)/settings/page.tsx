export default function SettingsPage() {
  return (
    <div className="mx-auto max-w-[760px] px-5 py-10">
      <h2 className="mb-2 text-[16px] font-semibold">Settings</h2>
      <p className="text-[13.5px] leading-relaxed text-mut">Settings would hold the calendar connection for each partner, the roster of executive assistants and which portfolio companies each one owns, the default meeting length, and the dinner time.</p>
      <p className="mt-4 text-[12px] text-mut">Not part of this demo.</p>
    </div>
  );
}
